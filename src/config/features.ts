/**
 * Временно скрытые разделы сайта.
 * Код и страницы остаются на месте — меняются только ссылки в меню/футере
 * и блоки на страницах. Чтобы вернуть раздел, поставь true.
 */
export const FEATURES = {
  /** Аренда зала: пункт меню, блок в услугах. Страница /hall-rental остаётся доступной по прямой ссылке. */
  hallRental: false,
  /** Тренеры: пункт меню. Страница /trainers остаётся доступной по прямой ссылке. */
  trainers: false,
} as const
